# GENERATED from jinja-templates/docker-compose.prod.yml.j2 —
# edit the template + setup.yml (variants.prod), then:
#   ansible-playbook jinja-gen/playbook.yml
# (env: {{ active_env_name }})
#
# POLARI RESEARCH FRAMEWORK - PRODUCTION
# Real-domain deployment: proxy-only ingress, resource limits,
# healthchecks. Run ./prod-setup.sh first to produce .generated/.
{% import 'compose_macros.jinja' as m %}
{% set v = variants.prod %}
{% set c = variants.common %}

services:
{{ m.mariadb(v.mariadb, c) }}

{{ m.keycloak(v.keycloak, c) }}

{{ m.proxy(v.proxy, c) }}

{{ m.file_store(v.file_store, c) }}

{{ m.frontend_staged(v.frontend, c) }}

{{ m.backend_staged(v.backend, c) }}

volumes:
  file-store-data:
  backend-data:
  prf-mariadb-data:

networks:
  {{ c.node_network }}:
    driver: bridge
